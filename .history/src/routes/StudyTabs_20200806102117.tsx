import React from 'react';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

const { Navigator, Screen } = createBottomTabNavigator();

function StudyTabs() {
  <Navigator>
    <Screeen name="TeacherLista"/>
  </Navigator>
}

export default StudyTabs;